# NPM Audit Notes (2026-04-07)

This note records the npm dependency audit results observed on **2026-04-07** for:

- `ui-tauri-react` in this repository
- Environment: Ubuntu 26.04 (resolute), Node 22.22.1, npm 9.2.0

## Context

The main Zenbook Duo runtime (Rust daemon + session agent) installed and runs via systemd.
The npm audit warnings are from the UI build dependency tree and do not by themselves indicate a failing runtime service.

## Observed Audit Output

Commands used:

```bash
cd ~/Projects/zenbook-duo-linux-main/ui-tauri-react
npm audit --omit=dev
npm audit
```

Results:

- `npm audit --omit=dev`: **4 high**
- `npm audit`: **7 total** (`2 moderate`, `5 high`)

Reported packages included:

- `@isaacs/brace-expansion` (high)
- `minimatch` (high)
- `path-to-regexp` (high)
- `picomatch` (high)
- `ajv` (moderate, full audit)
- `brace-expansion` (moderate, full audit)
- `flatted` (high, full audit)

`npm` reported fixes available via:

```bash
npm audit fix
```

## Important Security Note

During troubleshooting, the shell had:

```bash
NODE_TLS_REJECT_UNAUTHORIZED=0
```

This weakens TLS certificate verification and should remain unset.

Action already applied:

```bash
unset NODE_TLS_REJECT_UNAUTHORIZED
```

## Recommended Handling Strategy

1. Keep current runtime setup as-is if system behavior is stable.
2. Treat npm audit findings as dependency hygiene work for UI/build chain.
3. If/when updating, do it in a controlled pass:

```bash
cd ~/Projects/zenbook-duo-linux-main/ui-tauri-react
npm audit fix
npm audit
```

4. Rebuild and verify after dependency changes:

```bash
npm run build
```

5. Re-test core Zenbook functions (screen mode switching, keyboard detach behavior, session login stability) before reboot cycles.

